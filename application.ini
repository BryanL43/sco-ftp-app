; Application metadata shared by the main application, updater,
; installer, and GitHub Actions workflow.
;
; Runtime application settings must not be stored in this file.
; Use config.ini for user-configurable settings.
;
; Patch versions are generated automatically from existing Git tags.
; Only update the major and minor versions when creating a new release line.

[app]
name=ScoFTPTool
entry=src/Launcher.py

; Release version information.
; Increment major or minor as needed.
major=1
minor=0
